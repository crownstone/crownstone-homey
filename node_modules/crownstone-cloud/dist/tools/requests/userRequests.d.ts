import { RequestorBase } from "../requestorBase";
export declare class UserRequests extends RequestorBase {
    login(): Promise<cloud_LoginReply>;
    getKeys(): Promise<cloud_Keys[]>;
    getUserData(): Promise<cloud_UserData>;
    getUserId(): Promise<string>;
    getCurrentLocation(userId: any): Promise<cloud_UserLocation>;
}
