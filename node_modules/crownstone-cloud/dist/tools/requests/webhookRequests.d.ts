import { RequestorBase } from "../requestorBase";
export declare class WebhookRequests extends RequestorBase {
    isListenerActive(token?: string, userId?: string): Promise<boolean>;
    createListener(userId: string, token: string, eventTypes: string[], url: string): Promise<void>;
    getListeners(): Promise<cloud_EventListener[]>;
    deleteListenerByToken(token: string): Promise<void>;
    deleteListenerByUserId(userId: string): Promise<void>;
}
