import { Toolchain } from "./tools/toolchain";
import { WebhookRequestorInterface } from "./tools/requestors";
export declare class CrownstoneWebhooks {
    log: Logger;
    toolchain: Toolchain;
    rest: WebhookRequestorInterface;
    constructor(customEndpoint?: string);
    setApiKey(apiKey: string): void;
    setAdminKey(adminKey: string): void;
    isListenerActiveByToken(token: string): Promise<boolean>;
    isListenerActiveByUserId(userId: string): Promise<boolean>;
    createListener(userId: string, token: string, eventTypes: string[], url: string): Promise<void>;
    getListeners(): Promise<cloud_EventListener[]>;
    removeListenerByToken(token: string): Promise<void>;
    removeListenerByUserId(userId: string): Promise<void>;
}
